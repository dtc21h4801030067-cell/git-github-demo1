name: Kiem tra du an

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check-files:
    runs-on: ubuntu-latest

    steps:
      - name: Lay ma nguon
        uses: actions/checkout@v4

      - name: Kiem tra cac tep bat buoc
        run: |
          test -f index.html
          test -f style.css
          test -f script.js
          test -f README.md